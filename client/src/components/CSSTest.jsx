import React from 'react';

const CSSTest = () => {
    return (
        <div className="css-test">
            <h1>🎨 CSS Test - If you can see this styled, CSS is working!</h1>
            <p>This should have a gradient background, white text, and rounded corners.</p>
            <button className="btn btn-primary">Test Button</button>
        </div>
    );
};

export default CSSTest;
